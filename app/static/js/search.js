// let search_block = document.getElementById( `search_block` );

// search_block.addEventListener( `submit`, e => {

//     e.preventDefault();
// } );

// let input = search_block.querySelector( `input` );
// let select = search_block.querySelector( `select` );


// const resultBlock = document.querySelector( `.custom-seaarch-result` );

// 


// window.addEventListener( `click`, e => {
    
//     if( !document.getElementById( `my_custom_search` ).contains( e.target ) ){
//         resultBlock.classList.add( `search-hide` );
//     }
// } )